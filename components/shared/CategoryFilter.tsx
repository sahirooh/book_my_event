import {
  Select,
  SelectContent,
  SelectItem,
  SelectTrigger,
  SelectValue,
} from "@/components/ui/select";

const CategoryFilter = () => {
  return (
  <Select>
    <SelectTrigger>
        <SelectValue></SelectValue>
    </SelectTrigger>
  </Select>
    );
};

export default CategoryFilter;
